function obj = osivread(filename)
%OSIVREAD Read in an OSIV data file 
%   OBJ = OSIVREAD(FILENAME) reads in an OSIV data file and creates
%   a structure that contains the parameters that were used, the
%   prescribed interrogation positions, the calculated displacements,
%   and the calculated signal-to-noise ratio.  This structure has the
%   same form as that generated by the osivcorr/execute() function.
%

% Copyright (c) 2007-2009 James A. Strother


% Check arguments
if isa(filename, 'char')
   if ndims(filename) ~= 2 || size(filename,1) ~= 1
      error('filename argument has invalid type');
   end
end

% Outsource the real work
obj = osiv_intr('corr_load_out', filename);

